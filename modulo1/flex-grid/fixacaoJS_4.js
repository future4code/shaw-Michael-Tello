

 function contaOcorrencias(arrayDeNumeros, numeroEscolhido) {
  // Escreva seu código aqui
 const result = arrayDeNumeros.filter((array)=>{
if (array === 4) {
  return `O número ${}`
} else {
  return ` Número não encontrado`
}
 })
}